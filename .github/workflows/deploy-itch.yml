# ⚠️ itch 频率政策（boss 指示 2026-09-08）：itch 账户被标记机器人，解封前此 workflow 保持停用；
# 解封后恢复本 workflow，且 itch 更新频率上限 = 每周一次（攒批推送，禁止逐 fix 连发）。
# 恢复方法：git revert 熔断提交，或手动删掉下面的 if 守卫。
# 原步骤已封存于提交历史（butler push build/web → zheyu-julie/reed-bird-watching-simulator:html5）。

name: Deploy to itch.io (PAUSED)

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: itch 更新暂停中
        run: echo "itch 通道已由 boss 指示暂停（防机器人标记升级）。解封：git revert 熔断提交后恢复原 workflow。"
